document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.lead-form').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const subject=encodeURIComponent('FurnaceCheckNow Lead - '+(data.get('zip')||''));const body=encodeURIComponent(`Name: ${data.get('name')}\nPhone: ${data.get('phone')}\nZIP: ${data.get('zip')}\nProblem: ${data.get('problem')}\nPreferred contact: ${data.get('contact')}\n\nSource: FurnaceCheckNow.com`);window.location.href=`mailto:intake@furnacechecknow.com?subject=${subject}&body=${body}`;}));});
